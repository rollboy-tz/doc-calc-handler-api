# render.yaml
services:
  - type: web
    name: eduasas-pdf-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true